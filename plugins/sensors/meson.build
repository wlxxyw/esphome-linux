# Sensors Plugin meson.build
# No external dependencies - uses Linux /proc and /etc/fstab

# Plugin sources
plugin_sources += files(
  'sensors_plugin.c',
)

message('Sensors plugin: using /proc for system monitoring')
